// import { generate, Template } from "@pdfme/generator";

// export default function makeReport(props: {template: Template, input}) {
//   const {template, input} = props;
//   generate({template, input}).then((pdf) => {
//     console.log(pdf);
//     const blob = new Blob([pdf.buffer], {type: 'application/pdf'});
//      window.open(URL.createObjectURL(blob));
//   });
// }
